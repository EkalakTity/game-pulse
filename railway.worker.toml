[build]
builder = "nixpacks"
buildCommand = "npx prisma generate --schema=packages/database/prisma/schema.prisma && npm run build --workspace=apps/worker"

[deploy]
startCommand = "npm run start --workspace=apps/worker"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
