[build]
builder = "nixpacks"
buildCommand = "npm ci && npx prisma generate --schema=packages/database/prisma/schema.prisma && npm run build --workspace=apps/worker"

[deploy]
startCommand = "npm run start --workspace=apps/worker"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
